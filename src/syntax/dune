(ocamllex lexer)
(menhir
 (modules parser)
 (flags
   --no-dollars
   --table
   --strict
   --explain
   --dump))

(library
  (name syntax)
  (instrumentation (backend bisect_ppx))
  (libraries
    zarith
    common))
